[project]
name = "khvol"
version = "0.1.0"
description = "KH Volume helper CLI"
requires-python = ">=3.10"
dependencies = [
    "zeroconf==0.148.0",
    "pyssc @ git+https://github.com/schwinn/pyssc.git@main",
]

[dependency-groups]
build = [
    "pyinstaller==6.14.1",
]

[tool.uv]
package = false
